# Template only. NEVER commit a real secret to git.
# Apply by piping `kubectl create secret` with values pulled from your secret
# manager (Vault, Doppler, AWS Secrets Manager, etc.). Example:
#
#   kubectl create secret generic chipin-service-secrets \
#     -n chipin \
#     --from-literal=SPRING_DATASOURCE_URL='jdbc:postgresql://...' \
#     --from-literal=SPRING_DATASOURCE_USERNAME='chipin' \
#     --from-literal=SPRING_DATASOURCE_PASSWORD='...' \
#     --from-literal=JWT_SECRET='...' \
#     --from-literal=RESEND_API_KEY='...'
#
apiVersion: v1
kind: Secret
metadata:
  name: chipin-service-secrets
  namespace: chipin
type: Opaque
stringData:
  SPRING_DATASOURCE_URL: "REPLACE_ME"
  SPRING_DATASOURCE_USERNAME: "REPLACE_ME"
  SPRING_DATASOURCE_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  RESEND_API_KEY: "REPLACE_ME"
